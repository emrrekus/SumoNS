using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SumoNS.Abstracts.Points
{
    public interface IPoint
    {
        // We created it to keep numbers for each character dynamically
        void TakePoint(int point);

    }
}
